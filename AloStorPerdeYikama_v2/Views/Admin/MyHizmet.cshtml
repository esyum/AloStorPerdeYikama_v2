@using AloStorPerdeYikama_v2.Models
@model IEnumerable<HizmetTuru>

@{
    ViewBag.Title = "Hizmet Türleri";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="content">
    <div class="page-inner">
        <div class="row">
            <div class="col-md-12">
                <div class="card">
                    <div class="card-header">
                        <div class="d-flex align-items-center">
                            <h4 class="card-title">Hizmet Türü Listesi</h4>
                            <button class="btn btn-primary btn-round ml-auto" onclick="location.href='@Url.Action("HizmetTuru_Create", "Admin")'" data-toggle="modal" data-target="#addRowModal">
                                <i class="fa fa-plus"></i>
                                Yeni Kayıt
                            </button>
                        </div>
                    </div>
                    <div class="card-body">

                        <div class="table-responsive table-bordered">
                            <table id="add-row" class="display table table-striped table-hover table-bordered mt--2">
                                <thead>
                                    <tr>
                                        <th>Resim</th>
                                        <th>Başlık</th>
                                        <th>Metin</th>
                                        <th>Tarih</th>
                                        <th style="width: 10%"></th>
                                    </tr>
                                </thead>
                                <tbody>

                                    @foreach (var item in Model)
                                    {
                                        <tr>
                                            <td class="text-center">
                                                @if (item.fotograf == null)
                                                {
                                                    <img src="~/assets/img/blogpost.jpg" alt="resim yüklenemedi" height=100 width=100 />
                                                }
                                                else
                                                {
                                                    <img src="data:image/png;base64,@System.Convert.ToBase64String(item.fotograf)" style="height:100px; width:100px;" />
                                                }

                                            </td>
                                            <td>@item.Title</td>
                                            <td>@item.TitleSub</td>
                                            <td>@item.OlusturmaTarihi</td>
                                            <td>
                                                <div class="form-button-action">
                                                    <button type="button" onclick="location.href='@Url.Action("HizmetTuru_Edit", "Admin",new { id=item.ID})'" data-toggle="tooltip" title="" class="btn btn-link btn-primary btn-lg" data-original-title="Düzenle">
                                                        <i class="fa fa-edit"></i>
                                                    </button>

                                                    <button type="button" onclick="location.href='@Url.Action("HizmetTuru_Delete", "Admin", new { id=item.ID})'" data-toggle="tooltip" title="" class="btn btn-link btn-danger" data-original-title="Sil">
                                                        <i class="fa fa-times"></i>
                                                    </button>

                                                </div>
                                            </td>
                                        </tr>
                                    }


                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>


